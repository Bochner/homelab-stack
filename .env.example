# Homelab Stack Environment Configuration
# Copy this file to .env and customize the values

# =============================================================================
# DOMAIN CONFIGURATION
# =============================================================================
# Your domain name (registered with Cloudflare)
DOMAIN=example.com

# Timezone for all services
TZ=America/New_York

# =============================================================================
# CLOUDFLARE CONFIGURATION (Required for SSL certificates)
# =============================================================================
# Your Cloudflare account email
CF_EMAIL=your-email@example.com

# Cloudflare API token with DNS edit permissions
# Create at: https://dash.cloudflare.com/profile/api-tokens
CF_API_TOKEN=your-cloudflare-api-token

# =============================================================================
# SERVICE PASSWORDS (Generate secure passwords)
# =============================================================================
# Pi-hole admin password
PIHOLE_PASSWORD=change-this-secure-password

# Keycloak database configuration
KEYCLOAK_DB_USER=keycloak
KEYCLOAK_DB_PASSWORD=change-this-secure-db-password

# Keycloak admin credentials
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=change-this-secure-admin-password

# =============================================================================
# WATCHTOWER EMAIL NOTIFICATIONS (Optional)
# =============================================================================
# Set WATCHTOWER_NOTIFICATIONS to 'email' to enable email notifications
# Leave empty or set to 'none' to disable
WATCHTOWER_NOTIFICATIONS=none

# Email notification settings (only needed if notifications enabled)
WATCHTOWER_EMAIL_FROM=
WATCHTOWER_EMAIL_TO=
WATCHTOWER_EMAIL_SERVER=
WATCHTOWER_EMAIL_PORT=587
WATCHTOWER_EMAIL_USER=
WATCHTOWER_EMAIL_PASSWORD=

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Never commit this file to version control
# 2. Use strong, unique passwords for all services
# 3. Secure your Cloudflare API token with minimal required permissions
# 4. Change default Traefik dashboard credentials in traefik/dynamic.yml
# 5. Consider enabling 2FA where supported

# =============================================================================
# QUICK SETUP GUIDE
# =============================================================================
# 1. Replace all placeholder values above
# 2. Generate secure passwords (use: openssl rand -base64 32)
# 3. Create Cloudflare API token with DNS edit permissions
# 4. Run: chmod +x setup.sh && ./setup.sh --wizard